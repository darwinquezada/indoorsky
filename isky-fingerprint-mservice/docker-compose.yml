# docker-compose.yml
version: '3.8'

volumes:
  userdb_vol:

networks:
  micro_network:
    external:
      name: micro_network

services:
  auth-api:
    container_name: cont-fingerprinting-mservice
    build:
      context: .
    ports:
    - "5003:5000"
    networks:
      - micro_network
    restart: always
